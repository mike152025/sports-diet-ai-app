{
  "name": "sports-diet-ai-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mobile",
    "web",
    "api",
    "shared",
    "firebase"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "express": "^4.17.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-native": "^0.64.0",
    "firebase": "^9.6.1"
  }
}